#include <stdio.h>
/**
 * main - Print alphabets lowercase and uppercase
 *
 * Return: always 0
 */
int main(void)
{
int n = 97;
int n = 65;

while (n <= 122)
{
putchar(n);
n++;
}
while (m <= 90)
{
	putchar(m);
}
putchar('\n');
return (0);
}
